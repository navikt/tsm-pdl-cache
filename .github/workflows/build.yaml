on:
  push:
    branches:
      - '**'
      -
permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  build:
    uses: navikt/teamsykmelding-github-actions-workflows/.github/workflows/spring-boot-app.yaml@feat/spring-boot
    with:
      app: 'tsm-pdl-cache'
